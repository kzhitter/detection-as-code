name: Manual Push Detections to Elastic

on: 
  workflow_dispatch:


jobs:
  elastic-detections-all:
    runs-on: ubuntu-latest
    env:
      ELASTIC_API_KEY: ${{ secrets.ELASTIC_API_KEY}}


    steps:
      
      - uses: actions/checkout@v4

      - run: python develop/toml_to_json.py
